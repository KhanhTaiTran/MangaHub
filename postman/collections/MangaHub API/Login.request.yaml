$kind: http-request
version: '1.0.0'
name: Login
description: Authenticate a user and receive a JWT token.
method: POST
url: "{{baseUrl}}/auth/login"
order: 3000
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "username": "khanhtai",
      "password": "khanhtaitran"
    }
