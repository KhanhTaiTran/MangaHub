$kind: http-request
version: '1.0.0'
name: 'Get Profile'
description: "Protected endpoint that returns the current authenticated user's profile."
method: GET
url: '{{baseUrl}}/api/profile'
order: 4000
headers:
  - key: Authorization
    value: 'Bearer {{jwtToken}}'
